{
    "name": "Station 10",
    "id": 691,
    "prepare": [],
    "tests": [
        {
            "type": "junit-compat",
            "command": "docker run --rm -t -i --volume $PWD:/app --name php-stations-test php-stations:latest vendor/bin/phpunit --group=station10 --log-junit ./test-outputs/station10.xml",
            "args": [],
            "shell": true,
            "reportFile": "/test-outputs/station10.xml"
        }
    ]
}
