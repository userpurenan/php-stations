{
    "name": "Station 11",
    "id": 692,
    "prepare": [],
    "tests": [
        {
            "type": "junit-compat",
            "command": "docker run --rm -t -i --volume $PWD:/app --name php-stations-test php-stations:latest vendor/bin/phpunit --group=station11 --log-junit ./test-outputs/station11.xml",
            "args": [],
            "shell": true,
            "reportFile": "/test-outputs/station11.xml"
        }
    ]
}
